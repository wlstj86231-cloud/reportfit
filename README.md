# 레포트핏

과제 제출 전에 필요한 PDF, 이미지, 파일명, 글자수, 참고문헌, ZIP 정리를 브라우저 안에서 처리하는 도구형 웹사이트입니다.

## 개발

```bash
npm install
npm run dev
```

## 빌드

```bash
npm run build
```

빌드 결과는 `dist/`에 생성됩니다. Cloudflare Pages에서는 빌드 명령을 `npm run build`, 출력 폴더를 `dist`로 지정하면 됩니다.
